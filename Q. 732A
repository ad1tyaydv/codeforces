#include <cctype>
#include <iostream>
#include <type_traits>
#include <vector>
#include <string>
#include <algorithm>
#include <stack>
#include <queue>
#include <deque>
#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {

    int n,k;
    cin>>n>>k;
    int count = 0;

    for(int i=1;i<=10;i++) {
        int total_cost = i * n;

        if(total_cost % 10 == 0 || total_cost % 10 == k) {
            cout<<i<<endl;
            return 0;
        }
    }
}
